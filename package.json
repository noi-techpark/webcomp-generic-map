{
  "name": "webcomp-generic-map",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "setup": "lerna bootstrap",
    "build": "lerna run --parallel --stream build",
    "watch": "lerna run --parallel --stream watch",
    "demo": "lerna run --parallel --stream demo"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "lerna": "^3.13.1"
  }
}
